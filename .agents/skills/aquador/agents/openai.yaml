interface:
  display_name: "Aquador"
  short_description: "Repo-specific patterns and workflows for aquador"
  default_prompt: "Use the aquador repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true