# Continue Here — Aquad'or v3.0 Reset

Resume with `/qualia-resume` or read this file and run `/qualia-build 2 --auto` continuation.

## Where we are

**Branch:** `v3.0-reset` (main is preserved at `017fe0b`; v3.0-reset has all the reset work)
**Milestone:** M2 · Storefront That Sells (currently)
**Phase:** 2 of 5 · Product Detail Page (planned, mid-Wave-1 build)

## Session summary

- M1 (Design Foundation) shipped — 22 substantive commits across 3 phases: token substrate, Next 16 + React 19 migration, three-product-type unification, 19 token-driven primitives + admin showcase + admin dashboard migration. Milestone closed via `/qualia-milestone`, M2 opened.
- M2 plans for all 5 phases written in parallel + plan-checker revision loop. DESIGN.md §10b added with commit-blocking constraints (no em-dashes, no emojis, Card-as-section banned, motion required, voice consistent).
- M2 Phase 2.1 Homepage shipped — 4 tasks, 4 commits + 2 gap-closure commits. Subscribers Supabase migration also applied live via Supabase MCP. `/api/email-capture` wired end-to-end. Verifier FAIL gaps (voice drift + missing FeaturedGrid error boundary + BrandStory overflow + AiConcierge header-overlap) all closed inline.
- M2 Phase 2.2 PDP **build in progress**. 2 of 3 Wave 1 tasks committed:
  - `e3c89d5` Task 1 — TrustBar primitive (shared across PDP / Cart / Checkout)
  - `8e4e009` Task 2 — ProductGallery with Dialog lightbox + hover crossfade
- Task 3 (NotesStory rename / StickyATC / RelatedCarousel / SocialProof) wrote its files to disk but was interrupted before commit. **Currently untracked** in `src/components/storefront/`:
  - `ProductNotesStory.tsx` (T3 took the rename path — kept homepage NotesStory intact)
  - `ProductActions.tsx`, `RelatedCarousel.tsx`, `SocialProof.tsx`, `StickyATC.tsx`
  - Plus `src/lib/supabase/product-service.ts` modified (probably `getProductOrdersCount` helper for SocialProof)

## Next actions (resume)

1. **Inspect what T3 left.** Open each of the 5 untracked files + the product-service.ts diff. Either commit verbatim if they pass §10b + the plan's verification contracts, OR rerun T3 with the salvaged context. Run:
   ```bash
   git diff src/lib/supabase/product-service.ts | head -50
   grep -rEn ' — | – ' src/components/storefront/{ProductNotesStory,ProductActions,RelatedCarousel,SocialProof,StickyATC}.tsx
   grep -cE '<Card[ >]' src/components/storefront/{ProductNotesStory,ProductActions,RelatedCarousel,SocialProof,StickyATC}.tsx
   grep -c 'position: fixed\|fixed bottom' src/components/storefront/StickyATC.tsx
   npx tsc --noEmit 2>&1 | grep -c "error TS"
   ```
   If clean, commit them as T3.
2. **Spawn Wave 2 = Task 4** — rewrite `src/app/products/[slug]/page.tsx` as magazine-spread PDP consuming all Wave 1 components. Plan section starts at `.planning/phase-2-plan.md:189`.
3. After T4 commits, transition Phase 2 to built, chain `/qualia-verify 2 --auto`.
4. Verify → handle gaps → auto-advance to Phase 2.3 (Shop / Category), then 2.4 (Cart + Checkout), then 2.5 (Builder + AI Concierge), then `/qualia-milestone --auto` closes M2 and opens M3.

## Open trackable items

- **`.planning/phase-1-verification.md` untracked** — homepage browser QA report from the verifier agent. Commit with M2 Phase 2.1 close OR fold into M2 milestone-close archive.
- **`.planning/DESIGN.md` modified** — likely stale tail edits from earlier (no obvious reason for diff). Diff it before deciding whether to commit or revert.
- **playwright-report/ and test-results/** untracked — local Playwright artifacts; should be in `.gitignore` (verify). Don't commit.
- **supabase/.temp/** untracked — Supabase CLI temp dir; should be in `.gitignore`.

## Key decisions in this session

- v3.0 substrate done as fresh `/qualia-new` arc, archiving the Apr-10 pre-state-machine `.planning/` to `.planning/archive/pre-v3-2026-04-10/`. 4 milestones to handoff (Design Foundation, Storefront That Sells, Admin Rebuild, Handoff).
- Editorial-luxury Levant-coded direction locked in DESIGN.md §1. OKLCH-only palette anchored on aged-gold. Cormorant Garamond + Newsreader + GeistSans (Geist via `geist` npm package because Next 14.2.35's bundled next/font/google catalogue predates Geist).
- Stack migration done as M1 Phase 1.2: Next 16 + React 19 + ESLint 9 flat config + middleware → proxy rename.
- §10b storefront constraints added mid-M2 planning per owner feedback. Plans regenerated; voice constants locked verbatim.
- M2 Phase 2.4 (Cart + Checkout) keeps Stripe-hosted Checkout Session redirect (no Payment Element migration this phase). Wallets via Stripe Dashboard config. Documented as Scope Change.
- Branch hygiene: `main` reset to `017fe0b` (pre-session HEAD); all v3 work lives on `v3.0-reset`.

## Blockers / known issues

- **Pre-deploy hook intermittently blocks bash** when Jest baseline drifts; ran cleanly in M2 P2.1 build but worth knowing.
- **Supabase migration applied live** for `subscribers` table, but `src/lib/supabase/types.ts` was not regenerated (Phase 2 builder used a narrow inline cast). Cleanup task at M2 close: run `npx supabase gen types typescript --linked > src/lib/supabase/types.ts` once project is linked.
- **`@react-three/fiber + drei` retained** because `ProductGallery` originally consumed them; the v3 ProductGallery (commit 8e4e009) uses Next/Image + Dialog primitive — three.js is unused now. Audit + remove in M2 close cleanup.

## Recent commit graph (filtered)

```
e3c89d5 feat(storefront): add shared TrustBar primitive for PDP + Cart/Checkout
8e4e009 feat(pdp): add ProductGallery with Dialog lightbox + hover crossfade
8672cfa fix(phase-1): browser QA gaps — BrandStory bleed overflow + AiConcierge header-overlap
a6dece5 fix(phase-1): align voice constants to plan + graceful FeaturedGrid fallback
2f69229 feat(homepage): FeaturedGrid + EmailCapture + AiConciergeEntry + page assembly (M2 P2.1 T4)
62222d4 feat(storefront): Hero + editorial sections + FadeUp helper (M2 P2.1 T3)
a6415e5 feat(api): add email-capture POST route backed by subscribers table
19bf527 feat(db): add subscribers table migration for homepage email capture
```

Plus 4 M2-P2.5-planner commits and the M2-open milestone commit further back.
