# Fix Report - 2026-05-25 16:32

**Symptom:** Speak with a live agent did not let the visitor actually keep talking to the live desk; product photos were too small; PDP still showed the "Read the composition" dropdown.
**Mode:** general
**Outcome:** fixed

## Feedback Loop
- Initial failing check: inspection of `src/components/ai/AiConciergeDrawer.tsx` showed the button only created a request and notification, with no visitor-side live message read/send path.
- Final passing check: `npm run build`

## Root Cause
`src/components/ai/AiConciergeDrawer.tsx:349` - the live-agent action inserted a waiting session and sent a notification, but never switched the drawer into a live chat transport; `live_chat_messages` SELECT is RLS-restricted, so the browser also could not safely read live agent replies directly.

## Patch
- Files changed: `src/components/ai/AiConciergeDrawer.tsx`, `src/app/api/live-chat/session/route.ts`, `src/app/api/live-chat/messages/route.ts`, `src/components/ui/ProductCard.tsx`, `src/components/storefront/ProductGallery.tsx`, `src/components/storefront/FeaturedCard.tsx`, `src/components/storefront/ProductNotesStory.tsx`.
- Change summary: added server-mediated live chat session/message routes, wired the concierge drawer to send and poll live messages after a live-agent request, enlarged contained product imagery in square containers, and removed the PDP composition dropdown.

## Verification
- `npx eslint src/components/ai/AiConciergeDrawer.tsx src/app/api/live-chat/session/route.ts src/app/api/live-chat/messages/route.ts src/components/ui/ProductCard.tsx src/components/storefront/ProductGallery.tsx src/components/storefront/FeaturedCard.tsx src/components/storefront/ProductNotesStory.tsx` - PASS with one existing React hydration-effect warning.
- `npm run type-check` - PASS.
- `npm run build` - PASS.

## Remaining Notes
- None.
