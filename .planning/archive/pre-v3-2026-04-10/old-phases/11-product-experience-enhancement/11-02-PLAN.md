---
phase: 11-product-experience-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/products/ProductInfo.tsx
  - src/app/products/[slug]/page.tsx
  - src/components/products/AddToCartButton.tsx
autonomous: true

must_haves:
  truths:
    - "Product details display with luxury typography and spacing from Phase 10"
    - "Product page layout feels premium and trustworthy"
    - "Brand presentation matches high-end perfume standards"
  artifacts:
    - path: "src/components/products/ProductInfo.tsx"
      provides: "Luxury product information display"
      min_lines: 100
      contains: "font-playfair"
    - path: "src/app/products/[slug]/page.tsx"
      provides: "Enhanced product page layout"
      contains: "content-container"
  key_links:
    - from: "src/components/products/ProductInfo.tsx"
      to: "src/app/globals.css"
      via: "Phase 10 typography and color classes"
      pattern: "text-\\[clamp|oklch|tracking-"
---

<objective>
Elevate ProductInfo and product page layout with Phase 10 design system - fluid typography, OKLCH colors, refined spacing, and luxury brand presentation.

Purpose: Product pages are the decision point for high-end purchases. Typography, spacing, and visual hierarchy must convey luxury and build trust.
Output: Premium product details section and page layout using Phase 10 foundation (fluid type, OKLCH gold, spacing system, letter-spacing scale).
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-visual-foundation/10-01-SUMMARY.md
@.planning/phases/10-visual-foundation/10-02-SUMMARY.md
@~/.claude/skills/frontend-master/SKILL.md
@src/components/products/ProductInfo.tsx
@src/app/products/[slug]/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Phase 10 typography to ProductInfo</name>
  <files>
    src/components/products/ProductInfo.tsx
  </files>
  <action>
Replace static font sizes with Phase 10 fluid typography and enhance visual hierarchy:

**Brand label (currently text-xs):**
- Replace: text-xs → text-[clamp(0.625rem,0.5625rem+0.3125vw,0.75rem)]
- Add: tracking-[0.15em] (micro letter-spacing from Phase 10)
- Use: text-gold-500 (OKLCH gold from Phase 10)

**Product name (currently text-3xl md:text-4xl):**
- Replace: text-3xl md:text-4xl → text-[clamp(2rem,1.5rem+2.5vw,3.5rem)]
- Add: font-playfair font-semibold (weight 600 for luxury)
- Add: tracking-tight (Phase 10 display text spacing)
- Keep: text-white

**Price (currently text-3xl):**
- Replace: text-3xl → text-[clamp(2rem,1.75rem+1.25vw,2.75rem)]
- Change: font-playfair → font-playfair font-medium (weight 500 as "luxury class")
- Use: text-gold-600 (OKLCH gold-600 for price emphasis)

**Section labels ("Description", "Tags"):**
- Replace: text-sm → text-[clamp(0.75rem,0.6875rem+0.3125vw,0.875rem)]
- Add: tracking-[0.1em] (wide letter-spacing)
- Keep: uppercase

**Product detail pills:**
- Use: bg-dark-lighter/80 backdrop-blur-sm for atmospheric depth
- Border: border-gold-500/20 (OKLCH gold)
- Spacing: use px-5 py-2.5 (Phase 10 component spacing)
- Text: tracking-normal (not tight, easier to read)

**Overall spacing:**
- Main container: space-y-6 → space-y-8 (more breathing room)
- Borders: border-gold/10 → border-gold-500/10 (OKLCH)
- Padding: pt-4 → pt-6 for sections
  </action>
  <verify>
npm run build && grep -q "clamp" src/components/products/ProductInfo.tsx && grep -q "tracking-\\[0.15em\\]" src/components/products/ProductInfo.tsx && grep -q "gold-600" src/components/products/ProductInfo.tsx
  </verify>
  <done>
ProductInfo uses Phase 10 fluid typography (clamp() scaling), letter-spacing scale (0.15em micro to tight display), OKLCH gold colors (500/600), enhanced spacing (space-y-8), and luxury font weights (medium 500, semibold 600).
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance product page layout with Phase 10 spacing</name>
  <files>
    src/app/products/[slug]/page.tsx
  </files>
  <action>
Apply Phase 10 spacing system and layout refinements to product page:

**Main container:**
- Keep: min-h-screen bg-gold-ambient
- Update: pt-32 md:pt-40 lg:pt-44 → pt-[var(--nav-height-mobile)] md:pt-[var(--nav-height)]
- Update: pb-20 → pb-[var(--spacing-section-mobile)] md:pb-[var(--spacing-section)]
- Change: container-wide → max-w-[1400px] mx-auto px-6 md:px-8 (use content-container utility)

**Breadcrumb:**
- Add: tracking-normal to link text
- Enhance hover: hover:text-gold → hover:text-gold-500 transition-colors duration-200
- Icon size: w-4 h-4 → w-5 h-5 (better visual weight)

**Product grid:**
- Keep: grid-cols-1 lg:grid-cols-2
- Update: gap-12 lg:gap-16 → gap-[var(--spacing-lg)] lg:gap-[var(--spacing-xl)]

**Product details container (right column):**
- Update: space-y-8 → space-y-[var(--spacing-lg)]

**Additional info section (shipping/secure/delivery icons):**
- Border: border-gold/10 → border-gold-500/10
- Spacing: space-y-4 → space-y-5 (slightly more generous)
- Text: text-gray-400 → text-gray-300/90 (slightly brighter for readability)
- Icon color: text-gold → text-gold-500
- Add: backdrop-blur-sm to icon container divs for luxury depth
- Font size: text-sm → text-[clamp(0.875rem,0.8125rem+0.3125vw,1rem)]

**Visual polish:**
- Add subtle animation on page load: fade-in with stagger between columns
- Use Framer Motion <motion.div> for ProductGallery and ProductInfo containers
- Stagger delay: gallery 0s, info 0.1s
- Animation: opacity 0→1, y 20→0, duration 0.5s, ease-out
  </action>
  <verify>
npm run build && grep -q "content-container" src/app/products/[slug]/page.tsx && grep -q "var(--spacing" src/app/products/[slug]/page.tsx && grep -q "motion.div" src/app/products/[slug]/page.tsx
  </verify>
  <done>
Product page uses Phase 10 spacing vars (--spacing-lg/xl/section), content-container max-width (1400px), OKLCH gold colors, refined typography, and subtle fade-in animations with staggered timing for luxury presentation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Polish AddToCartButton with Phase 10 standards</name>
  <files>
    src/components/products/AddToCartButton.tsx
  </files>
  <action>
Apply Phase 10 design system to AddToCartButton for consistency:

**Typography:**
- Button text: text-base → text-[clamp(0.875rem,0.8125rem+0.3125vw,1rem)]
- Add: tracking-[0.05em] (slightly wide for luxury button text)
- Keep: font-medium

**Button styling:**
- Background: use OKLCH gold gradient - bg-gradient-to-r from-gold-500 to-gold-600
- Hover: hover:from-gold-600 hover:to-gold-700
- Shadow: add shadow-lg shadow-gold-500/20 for luxury depth
- Border radius: rounded-lg → rounded-xl
- Padding: px-8 py-4 → px-10 py-5 (more generous touch target)
- Ensure min-h-[44px] on mobile (Phase 10 touch target standard)

**Focus states:**
- Add: focus-visible:ring-2 focus-visible:ring-gold-500 focus-visible:ring-offset-2 focus-visible:ring-offset-dark

**Quantity selector:**
- Button size: w-8 h-8 → w-10 h-10 (better touch targets)
- Border: border-gold/30 → border-gold-500/30
- Hover: hover:border-gold → hover:border-gold-500 hover:bg-gold-500/10
- Ensure 44px touch targets on mobile

**Success state (green confirmation):**
- Keep existing animation
- Update green: bg-green-600 → bg-green-500/90 backdrop-blur-sm
- Text: add tracking-[0.05em] for consistency

**Disabled state:**
- Add: disabled:opacity-40 disabled:cursor-not-allowed
- Gray out when not in stock
  </action>
  <verify>
npm run build && grep -q "gold-500" src/components/products/AddToCartButton.tsx && grep -q "tracking-\\[0.05em\\]" src/components/products/AddToCartButton.tsx && grep -q "min-h-\\[44px\\]" src/components/products/AddToCartButton.tsx && npx tsc --noEmit
  </verify>
  <done>
AddToCartButton uses Phase 10 fluid typography, OKLCH gold gradient (500→600), 44px mobile touch targets, luxury spacing (px-10 py-5), proper focus states (ring-gold-500), and consistent letter-spacing (0.05em) across all states.
  </done>
</task>

</tasks>

<verification>
Manual verification after all tasks:
1. Visit http://localhost:3000/products/[any-product-slug]
2. Check typography scales smoothly when resizing browser (375px to 1440px+)
3. Verify brand label uses micro letter-spacing (0.15em) and looks refined
4. Verify product name feels luxurious with semibold weight and tight tracking
5. Verify price uses medium weight (500) and gold-600 OKLCH color
6. Check spacing feels generous and intentional (no cramped sections)
7. Test AddToCartButton - verify 44px+ touch target on mobile
8. Verify fade-in animation on page load (gallery → info stagger)
9. Check focus states - all interactive elements show gold ring on tab navigation
10. Verify WCAG contrast on all text elements
</verification>

<success_criteria>
1. Typography scales fluidly from 375px to 1440px+ without breakpoint jumps
2. Letter-spacing enhances luxury feel (micro on labels, tight on display text)
3. OKLCH gold colors appear perceptually consistent across page
4. Spacing feels generous and premium (not cramped)
5. All touch targets meet 44px minimum on mobile
6. Focus states are visible and use gold ring
7. Page load animation adds polish without performance impact
</success_criteria>

<output>
After completion, create `.planning/phases/11-product-experience-enhancement/11-02-SUMMARY.md`
</output>
