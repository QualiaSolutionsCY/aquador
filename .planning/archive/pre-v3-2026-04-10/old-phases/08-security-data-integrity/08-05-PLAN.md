---
phase: 08-security-data-integrity
plan: 05
type: tdd
wave: 2
depends_on: [08-01]
files_modified:
  - src/app/api/webhooks/stripe/__tests__/route.test.ts
autonomous: true

must_haves:
  truths:
    - "Stripe webhook test suite covers happy path checkout completion"
    - "Stripe webhook test suite covers duplicate event handling (idempotency)"
    - "Stripe webhook test suite covers malformed metadata parsing"
    - "Stripe webhook test suite covers email sending failures"
    - "All webhook tests pass with 100% coverage of critical paths"
  artifacts:
    - path: "src/app/api/webhooks/stripe/__tests__/route.test.ts"
      provides: "Comprehensive webhook test coverage"
      min_lines: 250
      exports: ["describe", "it", "expect"]
  key_links:
    - from: "route.test.ts"
      to: "src/app/api/webhooks/stripe/route.ts"
      via: "imports POST handler"
      pattern: "import.*route"
---

<objective>
Create comprehensive test suite for Stripe webhook handler (513 lines, currently zero tests) covering happy path, edge cases, and failure scenarios.

Purpose: Meet TEST-01 requirement — 10+ test cases for webhook reliability
Output: Full test coverage for webhook payment processing and email delivery
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/webhooks/stripe/route.ts
@.planning/phases/04-webhook-metadata-reconstruction/04-01-SUMMARY.md
</context>

<feature>
  <name>Stripe webhook payment processing</name>
  <files>
    - src/app/api/webhooks/stripe/route.ts
    - src/app/api/webhooks/stripe/__tests__/route.test.ts
  </files>
  <behavior>
**Expected behavior:**

1. **Happy path (cart checkout):**
   - Input: Valid checkout.session.completed event with cart metadata
   - Output: 200 response, order persisted, confirmation email sent

2. **Happy path (custom perfume):**
   - Input: Valid event with custom perfume metadata
   - Output: 200 response, order persisted, correct email template

3. **Duplicate event handling:**
   - Input: Same session_id sent twice
   - Output: 200 response both times, only one order created (idempotency)

4. **Malformed metadata:**
   - Input: Event with invalid JSON in metadata.items
   - Output: 200 response (Stripe always gets 200), error logged to Sentry, no crash

5. **Missing email:**
   - Input: Valid event but customer_details.email is null
   - Output: 200 response, order not created, graceful handling

6. **Email send failure:**
   - Input: Valid event, email service returns error
   - Output: 200 response (Stripe gets success), order persisted, error logged to Sentry

7. **Invalid signature:**
   - Input: Event with wrong Stripe signature header
   - Output: 400 response, webhook rejected

8. **Missing metadata:**
   - Input: Event with no metadata.items or metadata.perfume
   - Output: 200 response, fallback handling

9. **Product lookup failure:**
   - Input: Cart items reference non-existent product IDs
   - Output: 200 response, items skipped gracefully

10. **Database write failure:**
    - Input: Valid event, Supabase insert fails
    - Output: 200 response, error logged, no crash

**Test structure:**
- Mock Stripe signature verification
- Mock Supabase admin client
- Mock fetch (for email API)
- Mock getProductById (for cart reconstruction)
- Mock Sentry
  </behavior>
  <implementation>
Use Jest with mocked dependencies:

1. **Setup mocks:**
   - Mock @/lib/stripe with getStripe returning mock Stripe client
   - Mock @/lib/supabase/admin with createAdminClient
   - Mock @/lib/supabase/product-service with getProductsByIds
   - Mock global fetch for email API calls
   - Mock @sentry/nextjs

2. **Test helper:**
   - `createMockRequest(body, headers)` — creates NextRequest for webhook
   - `createMockEvent(overrides)` — creates valid Stripe event object

3. **Test cases:**
   - Group into describe blocks: "Cart checkout", "Custom perfume", "Error handling", "Edge cases"
   - Each test: arrange mocks, call POST(request), assert response + side effects

4. **Coverage targets:**
   - Lines: >80%
   - Branches: >70% (cover main error paths)
   - Focus: Critical path (order creation, email sending), idempotency, graceful degradation

Refer to Phase 4 SUMMARY for webhook metadata reconstruction pattern (shortened metadata format).
  </implementation>
</feature>

<verification>
- [ ] Test file created with 10+ test cases
- [ ] Tests cover happy path, duplicates, malformed data, failures
- [ ] `npm test` passes all webhook tests
- [ ] Coverage report shows >80% line coverage for route.ts
</verification>

<success_criteria>
**Measurable outcomes:**
1. Test file exists at `src/app/api/webhooks/stripe/__tests__/route.test.ts`
2. File contains 10+ test cases (count via `it(` or `test(` occurrences)
3. `npm test -- route.test.ts` exits with code 0 (all tests pass)
4. `npm run test:coverage` shows >80% line coverage for webhook route
5. Files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-data-integrity/08-05-SUMMARY.md`
</output>
