---
phase: 08-security-data-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sentry.server.config.ts
  - sentry.edge.config.ts
autonomous: true

must_haves:
  truths:
    - "Sentry does NOT send customer email addresses to third party"
    - "Sentry does NOT send customer IP addresses to third party"
    - "Sentry does NOT send cookie data to third party"
    - "Sentry trace sample rate is 10% in production (not 100%)"
  artifacts:
    - path: "sentry.server.config.ts"
      provides: "GDPR-compliant Sentry config"
      contains: "sendDefaultPii: false"
    - path: "sentry.edge.config.ts"
      provides: "GDPR-compliant Sentry config"
      contains: "sendDefaultPii: false"
  key_links:
    - from: "Sentry SDK initialization"
      to: "Sentry cloud"
      via: "sendDefaultPii: false prevents PII transmission"
      pattern: "sendDefaultPii:\\s*false"
---

<objective>
Configure Sentry to respect GDPR by disabling PII transmission and optimize production trace sampling.

Purpose: Eliminate HIGH security vulnerability (GDPR violation) and reduce unnecessary monitoring overhead
Output: Updated Sentry configs with sendDefaultPii: false and tracesSampleRate: 0.1
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@sentry.server.config.ts
@sentry.edge.config.ts
</context>

<tasks>

<task type="auto">
  <name>Update Sentry server config for GDPR compliance</name>
  <files>sentry.server.config.ts</files>
  <action>
Modify sentry.server.config.ts:

1. **Change sendDefaultPii** from `true` to `false` (line 18):
   ```typescript
   sendDefaultPii: false,
   ```

2. **Add environment-aware trace sampling** (replace line 11):
   ```typescript
   tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1,
   ```

3. **Add comment explaining GDPR compliance**:
   ```typescript
   // GDPR compliance: Do not send customer PII (IPs, emails, cookies) to third party
   sendDefaultPii: false,
   ```

Current config has `sendDefaultPii: true` (line 18) — this sends customer IP addresses, emails from error contexts, and cookie data to Sentry servers. GDPR violation for EU customers.

Current `tracesSampleRate: 1` (line 11) means 100% of requests are traced — excessive cost and data for production.
  </action>
  <verify>
Check file contains: `grep "sendDefaultPii: false" sentry.server.config.ts`
Check sample rate: `grep "tracesSampleRate:.*0.1" sentry.server.config.ts`
TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
sentry.server.config.ts has sendDefaultPii: false and production trace sampling at 10%.
No TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Update Sentry edge config for GDPR compliance</name>
  <files>sentry.edge.config.ts</files>
  <action>
Modify sentry.edge.config.ts (identical changes):

1. **Change sendDefaultPii** from `true` to `false` (line 19):
   ```typescript
   sendDefaultPii: false,
   ```

2. **Add environment-aware trace sampling** (replace line 12):
   ```typescript
   tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1,
   ```

3. **Add same GDPR comment**:
   ```typescript
   // GDPR compliance: Do not send customer PII (IPs, emails, cookies) to third party
   sendDefaultPii: false,
   ```

Edge runtime runs for middleware and edge API routes — same GDPR requirements apply.
  </action>
  <verify>
Check file contains: `grep "sendDefaultPii: false" sentry.edge.config.ts`
Check sample rate: `grep "tracesSampleRate:.*0.1" sentry.edge.config.ts`
TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
sentry.edge.config.ts has sendDefaultPii: false and production trace sampling at 10%.
Both Sentry configs are GDPR-compliant.
  </done>
</task>

</tasks>

<verification>
- [ ] Both Sentry config files have `sendDefaultPii: false`
- [ ] Both config files have conditional trace sampling (10% prod, 100% dev)
- [ ] TypeScript compilation passes
- [ ] Comments explain GDPR compliance reasoning
</verification>

<success_criteria>
**Measurable outcomes:**
1. `sendDefaultPii: false` in both sentry.server.config.ts and sentry.edge.config.ts
2. `tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1` in both files
3. No TypeScript errors after changes
4. Files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-data-integrity/08-02-SUMMARY.md`
</output>
