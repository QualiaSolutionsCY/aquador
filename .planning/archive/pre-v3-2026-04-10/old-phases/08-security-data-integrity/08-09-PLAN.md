---
phase: 08-security-data-integrity
plan: 09
type: execute
wave: 3
depends_on: [08-06]
files_modified:
  - src/app/api/checkout/__tests__/route.test.ts
  - src/app/api/ai-assistant/__tests__/route.test.ts
  - src/app/api/blog/__tests__/route.test.ts
  - src/app/api/contact/__tests__/route.test.ts
  - src/app/api/health/__tests__/route.test.ts
  - src/app/api/heartbeat/__tests__/route.test.ts
autonomous: true

must_haves:
  truths:
    - "API test coverage increased from 2/14 routes to 8/14 routes"
    - "Critical payment routes have test coverage (checkout, create-perfume/payment)"
    - "AI assistant route has test coverage"
    - "All new tests pass in CI"
  artifacts:
    - path: "src/app/api/checkout/__tests__/route.test.ts"
      provides: "Checkout API tests"
      min_lines: 80
    - path: "src/app/api/ai-assistant/__tests__/route.test.ts"
      provides: "AI assistant tests"
      min_lines: 60
    - path: "src/app/api/blog/__tests__/route.test.ts"
      provides: "Blog API tests"
      min_lines: 80
  key_links:
    - from: "API test files"
      to: "API route handlers"
      via: "imports and tests GET/POST handlers"
      pattern: "import.*route"
---

<objective>
Expand API test coverage from 2/14 routes to 8/14 routes, focusing on payment, AI, and blog endpoints.

Purpose: Meet TEST-02 requirement — improve reliability and catch regressions
Output: Test suites for 6 additional API routes
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/checkout/route.ts
@src/app/api/ai-assistant/route.ts
@src/app/api/blog/route.ts
@src/app/api/contact/route.ts
@src/app/api/health/route.ts
@src/app/api/heartbeat/route.ts
</context>

<tasks>

<task type="auto">
  <name>Create checkout API tests</name>
  <files>src/app/api/checkout/__tests__/route.test.ts</files>
  <action>
Create test suite for Stripe checkout session creation:

**Test cases (5-6 tests):**
1. Valid cart checkout — creates Stripe session, returns 200 with session URL
2. Valid custom perfume checkout — creates session with correct metadata
3. Invalid request body — returns 400 with error message
4. Stripe API failure — returns 500, logs error to Sentry
5. Price validation failure — returns 400 if client price doesn't match server catalog
6. Empty cart — returns 400

**Mocks needed:**
- Mock Stripe client (getStripe)
- Mock Supabase product-service (getProductsByIds)
- Mock Sentry
- Mock NextRequest

**Pattern:**
```typescript
import { POST } from '../route';
import { NextRequest } from 'next/server';

jest.mock('@/lib/stripe');
jest.mock('@/lib/supabase/product-service');
jest.mock('@sentry/nextjs');

describe('POST /api/checkout', () => {
  it('creates Stripe session for cart checkout', async () => {
    // Arrange: mock successful Stripe session creation
    // Act: call POST with valid cart request
    // Assert: returns 200, session URL in response
  });

  // ... more tests
});
```

Reference Phase 1 and 2 plan summaries for checkout implementation details.
  </action>
  <verify>
Test file exists: `ls src/app/api/checkout/__tests__/route.test.ts`
Tests pass: `npm test -- route.test.ts`
Coverage: `npm run test:coverage` shows checkout route covered
  </verify>
  <done>
Checkout API has test coverage for happy path and error cases.
  </done>
</task>

<task type="auto">
  <name>Create AI assistant API tests</name>
  <files>src/app/api/ai-assistant/__tests__/route.test.ts</files>
  <action>
Create test suite for AI fragrance assistant:

**Test cases (4-5 tests):**
1. Valid message — calls OpenRouter, returns AI response
2. Missing message in request — returns 400
3. OpenRouter API failure — returns 500, logs error
4. Rate limit exceeded (if rate limiting enabled) — returns 429
5. Streaming response works (if using streaming)

**Mocks needed:**
- Mock OpenRouter API (fetch to AI endpoint)
- Mock catalogue data import
- Mock rate limiter
- Mock Sentry

**Key behavior to test:**
- AI receives full catalogue context
- Error handling for API failures
- Request validation

Reference CLAUDE.md: AI uses OpenRouter with Gemini 2.0 Flash model.
  </action>
  <verify>
Test file exists: `ls src/app/api/ai-assistant/__tests__/route.test.ts`
Tests pass: `npm test -- ai-assistant/route.test.ts`
  </verify>
  <done>
AI assistant API has test coverage for request handling and errors.
  </done>
</task>

<task type="auto">
  <name>Create blog API tests</name>
  <files>src/app/api/blog/__tests__/route.test.ts</files>
  <action>
Create test suite for blog CRUD operations:

**Test cases (6-8 tests):**
1. GET /api/blog — returns published posts
2. GET /api/blog?category=X — filters by category
3. GET /api/blog/[slug] — returns single post
4. GET /api/blog/[slug] for unpublished — returns 404 for non-admin
5. POST /api/blog — creates post (requires admin auth)
6. POST /api/blog without auth — returns 401
7. PUT /api/blog/[id] — updates post
8. DELETE /api/blog/[id] — deletes post (admin only)

**Mocks needed:**
- Mock Supabase client
- Mock admin authentication check
- Mock blog post data

**Focus:**
- Published vs unpublished post visibility
- Admin-only write operations
- Validation of post data
  </action>
  <verify>
Test file exists: `ls src/app/api/blog/__tests__/route.test.ts`
Tests pass: `npm test -- blog/route.test.ts`
  </verify>
  <done>
Blog API has test coverage for CRUD operations and auth checks.
  </done>
</task>

<task type="auto">
  <name>Create contact, health, and heartbeat API tests</name>
  <files>
    - src/app/api/contact/__tests__/route.test.ts
    - src/app/api/health/__tests__/route.test.ts
    - src/app/api/heartbeat/__tests__/route.test.ts
  </files>
  <action>
Create test suites for remaining utility routes:

**Contact API (3-4 tests):**
1. Valid contact form — sends email via Resend, returns 200
2. Invalid email — returns 400
3. Missing required fields — returns 400
4. Resend API failure — returns 500

**Health API (2 tests):**
1. GET /api/health — returns 200 with status: "ok"
2. Includes timestamp in response

**Heartbeat API (2 tests):**
1. GET /api/heartbeat — returns 200 with uptime
2. Response includes basic system info

These are simpler routes — fewer test cases needed.

**Mocks needed:**
- Mock Resend client (contact)
- Mock fetch (contact email API)
- Mock Sentry

Create 3 separate test files (one per route).
  </action>
  <verify>
All 3 test files exist: `ls src/app/api/{contact,health,heartbeat}/__tests__/route.test.ts`
All tests pass: `npm test`
Coverage report shows all 3 routes tested
  </verify>
  <done>
Contact, health, and heartbeat APIs have test coverage.
All 6 new API test files created.
  </done>
</task>

</tasks>

<verification>
- [ ] 6 new API test files created (checkout, ai-assistant, blog, contact, health, heartbeat)
- [ ] All tests pass: `npm test` exits with code 0
- [ ] Coverage increased: count test files in src/app/api (should be 8 total including existing 2)
- [ ] TypeScript compilation passes
- [ ] Tests cover happy path and error cases for each route
</verification>

<success_criteria>
**Measurable outcomes:**
1. 6 test files exist at specified paths
2. `npm test` exits with code 0 (all tests pass)
3. Count of API test files: `find src/app/api -name "*.test.ts" | wc -l` returns 8+
4. Coverage report shows tested routes (checkout, ai-assistant, blog, contact, health, heartbeat)
5. Files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-data-integrity/08-09-SUMMARY.md`
</output>
